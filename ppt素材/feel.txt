隐 消 抗 避


2012.6.9
有安全问题 但是找不到

针对什么样的问题 作品的目的 隐藏自己，不管有没有安全问题，不被发现，这也是一个解决办法。下来看怎么隐藏。哪个隐藏技术别人已经有了，现在有什么隐藏的办法。百度搜索结果列出。

隐匿的通信（NAT TOR 洋葱路由），隐什么，通信流量还是人，双方合作还是……目前有的东西。信息隐匿（表面上看到，实际上还有其他东西）。

技术：
IP地址变，自己怎么变。以某一种策略变（设计算法）。用户上网流量模式（正常流量模型），浏览网页，下载等。

有针对性的模拟服务，如果对网页感兴趣，则专门模拟这方面的，（域名，邮件），机制。判断出感兴趣的方面。

网中网的隐匿，伪装。honeyd。模拟无线网。有线无线结合起来。namp是否可以扫描无线网。

吸收电磁波，修改电磁波。除被动外，主动发干扰信号，
伪装服务，伪装网络，隐在一个干扰的环境中，像拓扑信号，隐藏在噪声的环境中，核心是隐藏自己。

被动嗅探（假的，修改过的），主动探测（扫描），自己的对抗措施。

在tcp/ip分层中，层次以一样，提出一个自己的隐匿的模型，协议，字段，加一些东西。另外一套协议体系架构模型。A是正常通信模型（正常tcp/ip通信），B是隐匿的通信模型（名字怎么取）（ip，端口，网络怎么伪装）（ip层怎么工作，源ip地址有专门的算法实现，模拟网中网，实现路由算法）（ip地址含义一样，工作机制不同）。重新设计隐匿网络协议模型。

正常tcp/ip通信出问题，自己的隐匿还能用？源ip地址的含义，产生都是由特殊的算法，协议的设计。（chu，phu扫描，两个交换机。）

应用层协议混淆技术。snip，模拟出成百上千的计算机在通信。这样就很难发现，ip地址即可做到。图形化的演示。测试结果截图，不影响正常通信。两台机子通信是靠什么判断的（ip还是端口号）。布置假目标，找不到真正的。

传输层怎么做到的，服务层混淆。

nat等现有产品，最大的缺点，列出我们提出的——现状分析

永不消失的电磁波。

并行的信息隐匿模型

上三层重新设置。虚假路由伪装算法。

不同伪装达到的效果。

chu,phu截图。（演示阶段）

有线——无线 模拟出无线的ip
有线——有线



